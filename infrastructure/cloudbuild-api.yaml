steps:
  - name: gcr.io/cloud-builders/docker
    args: ['build', '-f', 'Dockerfile', '-t', '${_IMAGE}', '.']
images: ['${_IMAGE}']
substitutions:
  _IMAGE: REGION-docker.pkg.dev/PROJECT_ID/jobpilot/jobpilot-api:IMAGE_TAG